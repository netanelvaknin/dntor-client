import { Button } from "@material-ui/core";
import { DaysPickerContainer } from "./DaysPickerStyle";

export const DaysPicker = () => {
  return <DaysPickerContainer></DaysPickerContainer>;
};

export default DaysPicker;
